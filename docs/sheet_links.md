Sheets have a few kinds of critera:
- `src_column_above` - takes a `column` and `value` to compare
- `src_column_below` - takes a `column` and `value` to compare
- `src_column_equals` - takes a `column` and `value` to compare